/**
 * Redux `generation.style` value when the user defined a style from a reference image
 * (vision analysis → style name + directive). Not a catalog / API "style preset" id.
 */
export const CUSTOM_STYLE_FROM_IMAGE_ID = "custom_from_reference_image";
