"use client";

/**
 * Thin integration wrapper — keeps the existing import path stable
 * while delegating all logic to the modular /components/warli/ system.
 */
import { WarliModal } from "@/components/warli";

export default WarliModal;
