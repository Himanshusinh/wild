"use client";

/**
 * Thin integration wrapper — keeps the homepage import path stable
 * while delegating logic to the modular /components/uppada/ system.
 */
import { UppadaModal } from "@/components/uppada";

export default UppadaModal;

