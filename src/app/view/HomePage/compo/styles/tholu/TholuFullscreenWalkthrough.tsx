"use client";

/**
 * Thin integration wrapper — keeps the homepage import path stable
 * while delegating logic to the modular /components/tholu/ system.
 */
import { TholuModal } from "@/components/tholu";

export default TholuModal;

