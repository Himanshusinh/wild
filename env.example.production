# ============================================
# PRODUCTION ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env.production and fill in your production values
# DO NOT commit .env.production to version control
# Use your deployment platform's environment variable settings

# ============================================
# API Configuration (REQUIRED)
# ============================================
# Backend API base URL - MUST be set
# This should point to your production API gateway
NEXT_PUBLIC_API_BASE_URL=https://api.wildmindai.com

# ============================================
# Storage Configuration (REQUIRED)
# ============================================
# Zata storage prefix URL - MUST be set
# This is the base URL for accessing files stored in Zata storage
NEXT_PUBLIC_ZATA_PREFIX=https://idr01.zata.ai/devstoragev1/

# ============================================
# Firebase Configuration (REQUIRED)
# ============================================
# Firebase web app configuration
# Get these values from Firebase Console > Project Settings > Your apps > Web app
NEXT_PUBLIC_FIREBASE_API_KEY=your-production-firebase-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-production-project-id
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=your-project.appspot.com
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=123456789
NEXT_PUBLIC_FIREBASE_APP_ID=1:123456789:web:abcdef

# ============================================
# Cookie Domain (Optional but Recommended)
# ============================================
# Cookie domain for session cookies
# Use leading dot for cross-subdomain sharing: .wildmindai.com
NEXT_PUBLIC_COOKIE_DOMAIN=.wildmindai.com

# ============================================
# Debug/Development (Optional)
# ============================================
# Enable API debug logging (set to false in production)
NEXT_PUBLIC_API_DEBUG=false

# ============================================
# Server-Side Only (Optional)
# ============================================
# Server-side API base URL (alternative to NEXT_PUBLIC_API_BASE_URL for server-side requests)
# API_BASE_URL=https://api.wildmindai.com

# Backend fallback base URL (used when API base equals current host to prevent recursion)
# BACKEND_FALLBACK_BASE=https://api.wildmindai.com

# Redis Configuration (if using Redis for rate limiting)
# REDIS_URL=redis://your-redis-host:6379
# REDIS_HOST=your-redis-host
# REDIS_PORT=6379
# REDIS_PASSWORD=your-redis-password

